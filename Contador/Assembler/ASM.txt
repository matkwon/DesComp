Setup:
LDI R0, $0
STA @0, R0
STA @2, R0
LDI R1, $1
STA @1, R1
LDI R1, $10
STA @10, R1

Loop:
LDA R0, @KEY2 #a
AND R0, @1
CEQ R0, @0
JEQ @Loop
JSR @Incremento
JMP @Loop

Incremento:
CLR @KEY2
LDA R0, @2
SOMA R0, @1
STA R0, @2
STA R0, @HEX0
STA R0, @LED9
RET